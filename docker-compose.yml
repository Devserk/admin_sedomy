services:

  adminsedomyapp:
    command: npm run dev
    build:
      context: .
      dockerfile: Dockerfile
    restart: "unless-stopped"
    ports:
      - "10004:10004"
    volumes:
      - .:/app
