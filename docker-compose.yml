services:

  adminsedomyapp:
    command: npm install vite --save-dev && npx vite dev
    build:
      context: .
      dockerfile: Dockerfile
    restart: "on-failure"
    ports:
      - "10004:10004"
    volumes:
      - .:/app
